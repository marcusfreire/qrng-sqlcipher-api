# Caminho do arquivo de banco (no container)
DB_PATH=/data/keys.db

# Caminho do arquivo SECRETO com a senha do SQLCipher (montado como Docker Secret)
DB_KEYFILE=/run/secrets/db_key

# Porta de exposição da API
API_PORT=8081

# Uvicorn workers (1 ou 2 para SQLite; evite >2)
UVICORN_WORKERS=1